<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Ludo Admin</title>
  <style>
    body { font-family: sans-serif; margin: 20px; }
    pre { background: #f0f0f0; padding: 10px; }
  </style>
</head>
<body>
  <h1>Ludo Admin Panel</h1>

  <h2>Warteschlangen:</h2>
  <ul>
    <li>2er-Queue: <%= waitingQueues["2"].length %> Spieler (<%= waitingQueues["2"] %>)</li>
    <li>4er-Queue: <%= waitingQueues["4"].length %> Spieler (<%= waitingQueues["4"] %>)</li>
  </ul>

  <h2>Laufende Räume:</h2>
  <% for (let roomId in rooms) { %>
    <div style="border: 1px solid #ccc; margin-bottom: 10px; padding: 10px;">
      <h3>Raum <%= roomId %> (Mode <%= rooms[roomId].mode %>)</h3>
      <p>Spieler: <%= rooms[roomId].players %></p>
      <p>Aktueller State:</p>
      <pre><%= JSON.stringify(rooms[roomId].gameState, null, 2) %></pre>
    </div>
  <% } %>

</body>
</html>
